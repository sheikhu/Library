    {% extends '@SheikhuLibrary/layout.html.twig' %}

    {% block content -%}
    {% embed '@SheikhuLibrary/Embed/grid.html.twig' with { size: 4} %}
    {% block title %}Membre creation {% endblock %}

    {% block grid_content %}
    {{ form_start(edit_form) }}
    <div class="mws-form-inline">
        {{ macros.render_form(edit_form, ['submit']) }}
    </div>

    <div class="mws-button-row">
        {{ form_widget(edit_form.submit, {attr: {class: 'btn btn-success'}}) }}
        {{ macros.btn_link_to('Retour à la liste', 'livres', {}, {'class': 'btn btn-info'}) }}
    </div>


    {% endblock %}

    {% block toolbar_bottom %}
        <div class="mws-panel-toolbar">
            <div class="btn-group dropup">
                {{ form_start(delete_form) }}
                {{ form_row(delete_form.submit, {'attr': {class: 'btn-danger'}}) }}
                {{ form_end(delete_form) }}
            </div>
        </div>
    {% endblock %}

    {% endembed %}
    {% endblock %}
