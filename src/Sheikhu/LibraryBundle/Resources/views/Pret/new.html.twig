{% extends '@SheikhuLibrary/layout.html.twig' %}

{% block content -%}
    {% embed '@SheikhuLibrary/Embed/grid.html.twig' with { size: 6} %}
    {% block title %}Pret creation - {{ membre.fullName }} {% endblock %}

    {% block grid_content %}
        {{ form_start(form) }}
    <div class="mws-form-inline">
        {{ form_row(form.membre) }}
        {{ form_row(form.exemplaires, {'attr': {'class': 'mws-select2'} } ) }}

        {{ form_row(form.dateRetourPrevue) }}
    </div>

        <div class="mws-button-row">
            {{ form_widget(form.submit, {attr: {class: 'btn btn-success'}}) }}
            {{ macros.btn_link_to('Retour à la liste', 'livres', {}, {'class': 'btn btn-info'}) }}
        </div>
        {{ form_end(form) }}

    {% endblock %}

    {% endembed %}
{% endblock %}
