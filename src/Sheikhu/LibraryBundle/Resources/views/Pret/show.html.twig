{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pret</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Datepret</th>
                <td>{{ entity.datePret|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Dateretourprevue</th>
                <td>{{ entity.dateRetourPrevue|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Dateretour</th>
                <td>{{ entity.dateRetour|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ entity.etat }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('prets') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('prets_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
