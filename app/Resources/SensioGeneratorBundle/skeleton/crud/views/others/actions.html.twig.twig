{% set icons = ['search', 'edit'] %}

<div class="btn-group">

    {%- for action in record_actions %}

        {{ "{{ macros.link_icon('" ~ action ~ "', '" ~ route_name_prefix ~ "_" ~ action ~"', { 'id': entity."~identifier~" }, '"~icons[loop.index-1]~"') }}" }}
    {%- endfor %}

</div>
